create table products(
  name CHAR(80) primary key,
  catergory CHAR(80),
  warehouse CHAR(80),
  quantity INT
);

create table orders(
  name CHAR(80) primary key,
  catergory CHAR(80),
  warehouse CHAR(80),
  quantity INT
);
